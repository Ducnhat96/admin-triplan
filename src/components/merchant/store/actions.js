/*import * as customer from 'src/api/customers'
import { getStatisticPurchase } from 'src/api/statistics'*/

/*const getCustomerStatistics = ({commit, dispatch}, params) => {
  dispatch('toggleLoadding', true, {root: true})
  getStatisticPurchase(params).then(response => {
    if (response.code === 200) {
      dispatch('toggleLoadding', false, {root: true})
      commit(SET_CUSTOMER_STATISTIC, response.data)
    } else {
      dispatch('toggleLoadding', false, {root: true})
    }
  })
}*/
export default{
  // getCustomerStatistics
}
